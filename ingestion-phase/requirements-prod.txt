# Production-optimized requirements (smaller image size)
# Core web framework
streamlit>=1.28.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0

# Database
pymongo>=4.0.0
motor>=3.3.0
dnspython>=2.3.0
chromadb>=0.4.0

# AI/ML - CPU-only PyTorch (much smaller than GPU version)
# Use CPU-only torch to reduce size from ~2GB to ~200MB
--index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
transformers>=4.30.0
huggingface-hub>=0.16.0
sentence-transformers>=2.2.0

# Data processing (minimal)
pandas>=1.5.0
numpy>=1.24.0

# API clients
groq>=0.4.0
python-dotenv>=1.0.0
requests>=2.28.0

# PDF generation
reportlab>=4.0.0
pypdf>=4.2.0
python-docx>=0.8.11

# Utilities
tqdm>=4.65.0

# Removed for production (not needed):
# - pyautogen (large, optional)
# - bert-score (large, optional)
# - matplotlib, seaborn, plotly (visualization, not needed in production)
# - scikit-learn (only needed if using ML features)
